---
label: Project
hide_body: false
display_field: title
fields:
- name: image-section
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  config:
    min: 0
    max: 2
    labelField: 
  label: Image Section
  description: " Upload up to 2 images."
- name: date
  type: datetime
  label: Date
  description: ''
  config:
    required: false
    date_format: 
    time_format: 
    display_utc: false
    export_format: MM-DD-YYYY
- name: title
  type: text
  config:
    required: false
  label: Title
- name: lead_proponent
  type: text
  config:
    required: false
  label: Lead Proponent
- name: co-proponents
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Co-proponents
- name: partner_agencies
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Partner Agencies
- name: layout
  type: text
  config:
    required: false
  label: Layout
  hidden: true
  default: project
pages:
- content/_projects/an-optimization-model-for-vaccine-allocation.md
